// Used smart quote for "let's" although design calls for undirected quote.
// I went with the majority (8 instances of smart quotes vs 2 undirected) to present a consistent style.
export const userHeader = (name: string) =>
  `${name}, let’s help you pay for college!`
